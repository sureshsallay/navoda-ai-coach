let bank=[],quiz=[],i=0,score=0,mode='',answers=[],started=0,te=false;const $=x=>document.getElementById(x);let P=JSON.parse(localStorage.getItem('navP2')||'{"attempts":0,"correct":0,"answered":0,"topics":{}}');
async function init(){bank=await fetch('questions.json?v=2', {cache:'no-store'}).then(r=>r.json());subjects();net();$('coach').textContent=coachText()}function net(){$('net').textContent=navigator.onLine?'🟢 Online + Offline-ready':'🔌 Offline mode'}addEventListener('online',net);addEventListener('offline',net);function hide(){['home','quiz','result','topics','progress'].forEach(x=>$(x).classList.add('hidden'))}function home(){hide();$('home').classList.remove('hidden');$('coach').textContent=coachText()}function shuffle(a){return [...a].sort(()=>Math.random()-.5)}function subjects(){let c={};bank.forEach(q=>c[q.subject]=(c[q.subject]||0)+1);$('subjects').innerHTML=Object.entries(c).map(([s,n])=>`<button class="secondary" onclick="subject('${s}')">${icon(s)} ${s}<br><span class="small">${n} questions</span></button>`).join('')}function icon(s){return s.includes('Mental')?'🧠':s.includes('Arithmetic')?'➗':s.includes('Language')?'📖':'🌱'}function start(a,m){mode=m;quiz=shuffle(a);i=0;score=0;answers=[];started=Date.now();hide();$('quiz').classList.remove('hidden');render()}function quick(){start(bank.slice(0), 'Quick Practice')}function mock(){start(bank.slice(0), 'Mock Test');quiz=quiz.slice(0,20)}function subject(s){start(bank.filter(q=>q.subject===s).slice(0),'Subject: '+s);quiz=quiz.slice(0,10)}function topic(t){start(bank.filter(q=>q.topic===t).slice(0),'Topic: '+t);quiz=quiz.slice(0,10)}function render(){let x=quiz[i];$('meta').textContent=`${mode} • ${i+1}/${quiz.length}`;$('bar').style.width=(i/quiz.length*100)+'%';$('label').textContent=`${icon(x.subject)} ${x.subject} • ${x.topic} • ${x.difficulty}`;$('q').textContent=te?(x.te_question||x.question):x.question;$('opts').innerHTML=x.options.map((o,n)=>`<button class="option" onclick="choose(${n})">${String.fromCharCode(65+n)}. ${o}</button>`).join('');$('next').disabled=true;$('exp').classList.add('hidden')}function choose(n){let x=quiz[i],bs=[...document.querySelectorAll('.option')];bs.forEach(b=>b.disabled=true);if(n===x.answer){score++;bs[n].classList.add('correct')}else{bs[n].classList.add('wrong');bs[x.answer].classList.add('correct')}answers.push({x,ok:n===x.answer});$('next').disabled=false;$('exp').classList.remove('hidden');$('exp').innerHTML=`<b>💡 ${te?(x.te_explanation||x.explanation):x.explanation}</b>`}function next(){i++;i>=quiz.length?finish():render()}function finish(){P.attempts++;P.correct+=score;P.answered+=quiz.length;answers.forEach(a=>{let t=a.x.topic;P.topics[t]??={correct:0,total:0};P.topics[t].total++;if(a.ok)P.topics[t].correct++});localStorage.setItem('navP2',JSON.stringify(P));let acc=Math.round(score/quiz.length*100),secs=Math.round((Date.now()-started)/1000);hide();$('result').classList.remove('hidden');$('stats').innerHTML=`<div class="stat">Score<b>${score}/${quiz.length}</b></div><div class="stat">Accuracy<b>${acc}%</b></div><div class="stat">Time<b>${Math.floor(secs/60)}m ${secs%60}s</b></div>`;$('rec').textContent=rec(acc);let t={};answers.forEach(a=>{let k=a.x.topic;t[k]??={c:0,n:0};t[k].n++;if(a.ok)t[k].c++});$('analysis').innerHTML=Object.entries(t).map(([k,v])=>`<div class="stat" style="margin:7px 0"><b style="font-size:18px">${k}</b>${v.c}/${v.n} correct — ${Math.round(v.c/v.n*100)}%</div>`).join('');$('coach').textContent=coachText()}function rec(a){return a>=90?'Excellent — move to harder sets and full mocks.':a>=75?'Strong — mix timed tests with weak-topic practice.':a>=50?'Good start — revise weak topics and try again.':'Keep practising — focus on explanations and weak topics.'}function coachText(){if(!P.answered)return'Take a test to get a recommendation.';let a=Math.round(P.correct/P.answered*100);return`Overall accuracy: ${a}%. ${rec(a)}`}function topics(){hide();$('topics').classList.remove('hidden');let ts=[...new Set(bank.map(q=>q.topic))].sort();$('topicList').innerHTML=ts.map(t=>`<button class="secondary" onclick="topic(${JSON.stringify(t)})">${t}</button>`).join('')}function progressPage(){hide();$('progress').classList.remove('hidden');let a=P.answered?Math.round(P.correct/P.answered*100):0;$('pstats').innerHTML=`<div class="stat">Attempts<b>${P.attempts}</b></div><div class="stat">Questions<b>${P.answered}</b></div><div class="stat">Accuracy<b>${a}%</b></div>`;let w=Object.entries(P.topics).sort((a,b)=>a[1].correct/a[1].total-b[1].correct/b[1].total).slice(0,6);$('weak').innerHTML=w.length?w.map(([t,v])=>`<div class="stat" style="margin:7px 0"><b style="font-size:18px">${t}</b>${v.correct}/${v.total} correct — ${Math.round(v.correct/v.total*100)}%</div>`).join(''):'No data yet.'}function resetP(){if(confirm('Reset progress on this device?')){P={attempts:0,correct:0,answered:0,topics:{}};localStorage.setItem('navP2',JSON.stringify(P));progressPage()}}function lang(){te=!te;alert(te?'తెలుగు ప్రశ్న మోడ్':'English question mode');if(!$('quiz').classList.contains('hidden'))render()}init();

if("serviceWorker" in navigator) navigator.serviceWorker.register("service-worker.js").catch(()=>{});
